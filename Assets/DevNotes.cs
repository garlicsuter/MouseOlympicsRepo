using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DevNotes : MonoBehaviour
{
    //To Do:
    // 1) Waypoint dots should only light up if START has been clicked. (if running == true)...
    // 2) UI to display countdown timer
    // 3) Each waypoint adds translate.rotation (45 degrees) to Mouse Cursor icon
    // 4) Once running == true, touching black is FAULT! UI text, stop countdown, subtract 1 from 3 attempts
    // 5.1) Reaching START (OnMouseOver) adds Impulse force to hammerball of (countdownTimeRemaining),
    // 5.2) Sets var and UI of "bestAttempt" float.
}
